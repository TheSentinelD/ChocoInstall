---
- name: Install z Chocolatey
  hosts: "{{ seznam_stanic }}"
  tasks: 
   - name: Install z Chocolatey
     win_chocolatey: 
      name: "{{ aplikace1 }}"
     state: "{{ stav }}"
   - name: Install z Chocolatey
     win_chocolatey: 
      name: "{{ aplikace2 }}"
     state: "{{ stav }}"
   - name: Install z Chocolatey
     win_chocolatey: 
      name: "{{ aplikace3 }}"
     state: "{{ stav }}"
